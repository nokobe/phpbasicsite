CREATE USER 'pbs'@'127.0.0.1' IDENTIFIED BY '***';

GRANT USAGE ON * . * TO 'pbs'@'127.0.0.1' IDENTIFIED BY '***' WITH MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0 ;

CREATE DATABASE IF NOT EXISTS `pbs` ;

GRANT ALL PRIVILEGES ON `pbs` . * TO 'pbs'@'127.0.0.1';

